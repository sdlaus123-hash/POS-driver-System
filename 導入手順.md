# POMS v2.1 導入手順

## 1. GASを貼り付ける

1. 対象スプレッドシートから「拡張機能 → Apps Script」を開きます。
2. 既存の`コード.gs`をバックアップします。
3. `apps_script.gs`の内容をすべて貼り付け、保存します。
4. Apps Scriptの「プロジェクトの設定 → スクリプト プロパティ」に以下を登録します。

| キー | 必須 | 値 |
| --- | --- | --- |
| `SPREADSHEET_ID` | 必須 | POMSスプレッドシートのID |
| `ADMIN_USERNAME` | 必須 | 例: `admin` |
| `ADMIN_PASSWORD` | 必須 | 初回管理者用4桁PIN |
| `LINE_CHANNEL_ACCESS_TOKEN` | LINE使用時 | 再発行したアクセストークン |
| `LINE_ADMIN_TO` | LINE使用時 | 管理者userIdまたはgroupId |
| `DRIVER_ATTENDANCE_URL` | 推奨 | Vercelの勤務報告URL |
| `DRIVER_ADVANCE_URL` | 推奨 | Vercelの前払いURL |

秘密情報をスプレッドシートのセルやGASソースへ直接書かないでください。

## 2. 初期設定を実行する

1. Apps Script上部の関数選択で`setupPomsOperationalRebuildNow`を選びます。
2. 「実行」を押し、Googleの権限を承認します。
3. スプレッドシートを開き直します。
4. 「POMS管理 → 運用ガイド / 設定診断」を実行します。
5. `POMS_運用ガイド`に「要設定」がないことを確認します。

初期設定は既存データを消さず、必要なシート、月次処理、定期トリガーを準備します。

## 3. GASをWebアプリとして公開する

1. Apps Script右上の「デプロイ → 新しいデプロイ」を開きます。
2. 種類は「ウェブアプリ」を選びます。
3. 実行するユーザーは「自分」、アクセスできるユーザーは運用条件に合わせて設定します。
4. デプロイ後の`/exec` URLをコピーします。
5. `config.js`の`API_BASE_URL`へ貼り付けます。

GASコードを変更した場合は「デプロイを管理 → 編集 → 新バージョン」で更新してください。保存しただけでは公開版へ反映されません。

## 4. Vercelへ配置する

このフォルダをGitHubへアップロードし、Vercelプロジェクトへ接続します。VercelのEnvironment Variablesへ以下を登録します。

| キー | 値 |
| --- | --- |
| `POMS_GAS_URL` | 上記GAS Webアプリの`/exec` URL |
| `LINE_CHANNEL_SECRET` | LINE Developersのチャネルシークレット |
| `GOOGLE_SHEETS_WEBHOOK_URL` | 採用応募保存を使う場合のみ |

環境変数を変更したらVercelを再デプロイします。

## 5. 日常管理

- ドライバー、単価、PIN、口座: `マスターデータ`
- 現場、前払い手数料: `現場管理`
- 月別の日報送信先: `日報送信先設定`
- 将来の現場変更: `現場異動予約`

現場異動は、ドライバー名、異動先現場、適用開始日、適用単価を入力します。適用終了日がなければ空欄にします。

## 6. 動作確認

1. ドライバーでログインします。
2. 出勤を保存し、すぐに完了画面になることを確認します。
3. 退勤を保存します。
4. 約30秒後に稼働台帳と管理カレンダーが更新されることを確認します。
5. 前払い画面で退勤済みの日だけ選択できることを確認します。
6. 管理画面で当月データを確認します。

集計が更新されない場合は、`flushPomsOperationalRefreshQueue`をApps Scriptから一度実行できます。

## 7. 毎月の更新

毎月1日の午前3時ごろに、新しい月のカレンダー、確定シフト、日報送信先が自動準備されます。手動実行する場合は`runPomsMonthlyRollover`を実行します。

